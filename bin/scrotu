#!/usr/bin/env bash

year=$(date +%Y)
name=$(zenity --entry --text Filename:)
[ "$name" = "" ] && name=$(mktemp -u | sed 's,^/tmp/,,')
scrot -s $HOME/tmp/$name.png
scp $HOME/tmp/$name.png bianca:~/www/stuff.art-core.org/public/test/
zenity --info --text=https://stuff.art-core.org/$year/$name.png
