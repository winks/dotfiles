unset http_proxy
unset https_proxy

unset HTTP_PROXY
unset HTTPS_PROXY

unset no_proxy
unset NO_PROXY

unset EQC_PROXY

echo "#######################"
echo "# env | grep -i proxy #"
env | grep -i proxy || echo "-empty-"
echo "#######################"
